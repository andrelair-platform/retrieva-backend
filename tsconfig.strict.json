{
  "// note": "Incremental strict gate (RTV-22..25). `strict:true` is enforced on the converted files listed in `include`; `allowJs:true` lets them still import not-yet-converted `.js` at the boundary (as loose types). Each conversion slice adds its files here; RTV-25 flips `allowJs:false`, folds this into the base tsconfig, and deletes this file. `npm run typecheck:strict` runs it; CI blocks on it. (db/tenantContext.ts + the repository subclasses are the next slice.)",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "strict": true,
    "allowJs": true,
    "checkJs": false,
    "noEmit": true
  },
  "include": [
    "db/schema/**/*.ts",
    "db/entityContext.ts",
    "db/migrate.ts",
    "db/queries/**/*.ts",
    "repositories/drizzle/BaseDrizzleRepository.ts",
    "types/**/*.d.ts"
  ],
  "exclude": ["node_modules", "dist", "coverage", "tests", "website", "seeds", "data"]
}
